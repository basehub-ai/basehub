---
"basehub": minor
"playground": patch
---

Add partial support for Next.js Cache Components. This allows doing BaseHub queries inside Cache Components without throwing errors, with the caveat that the toolbar branch selector won't work. This version doesn't change any behaviors for projects without Cache Components enabled.
